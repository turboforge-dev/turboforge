[**@turbo-forge/cli-kit**](../index.md)

***

[@turbo-forge/cli-kit](../index.md) / tryImport

# Function: tryImport()

> **tryImport**\<`T`\>(`p`): `Promise`\<`T` \| `null`\>

Defined in: [packages/cli-kit/src/utils.ts:49](https://github.com/react18-tools/turbo-forge/blob/main/packages/cli-kit/src/utils.ts#L49)

Tries to import a module. supports both Jiti (if available) and native import.

## Type Parameters

### T

`T` = `unknown`

## Parameters

### p

`string`

## Returns

`Promise`\<`T` \| `null`\>
