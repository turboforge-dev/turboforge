import { type LogLevel, createLogger, deepMerge } from "@turbo-forge/cli-kit";

export interface {{pascalCase fileName}}Options {
    logLevel?: LogLevel;
}

export const DEFAULT_CONFIG: Required<{{pascalCase fileName}}Options> = {
  logLevel: "info",
}

export const {{camelCase fileName}} = async (options: {{pascalCase fileName}}Options) => {
  const config = deepMerge(DEFAULT_CONFIG, options) as Required<{{pascalCase fileName}}Options>;
  const logger = createLogger({ level: config.logLevel });
  logger.info("Hello from {{ camelCase fileName }}!")
};
