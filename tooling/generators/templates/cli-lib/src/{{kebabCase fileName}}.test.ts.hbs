/** biome-ignore-all lint/suspicious/noExplicitAny: Use of any is ok for test files */

import { describe, test } from "vitest";
import { {{camelCase fileName}}, DEFAULT_CONFIG } from "./{{kebabCase fileName}}";

describe("{{camelCase fileName}}", () => {
  test("should work with default config", async ({ expect }) => {
    // Basic connectivity test
    await expect({{camelCase fileName}}(DEFAULT_CONFIG)).resolves.not.toThrow();
  });
});
