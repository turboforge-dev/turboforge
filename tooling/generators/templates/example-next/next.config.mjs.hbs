/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    transpilePackages: [
    {{#each dependencies}}
        "{{this}}",
    {{/each}}
    ]
};

export default nextConfig;
